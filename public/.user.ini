; Per-request limits for this app (read by PHP when handling requests under public/).
; Must cover collaboration deliverables (up to 100 MB) plus multipart overhead.
upload_max_filesize = 128M
post_max_size = 128M
max_execution_time = 600
max_input_time = 600
